
void alignToGenomeWrapper(Dataset &data, int iteration) {

  string genome = "hg19";
  vector<string> nucleotideStack = {"A", "C", "T", "G"};
  int length = 0;
}


// getlength accesses first(?) element of kmerHash and returns the key
int getLength(Dataset &data){
  
}
